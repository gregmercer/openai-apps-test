[project]
name = "todos-server-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.118.2",
    "mcp[fastapi]>=1.16.0",
    "uvicorn>=0.37.0",
]
